# 启动器前端界面需求提要

> 文档用途：用于说明启动器本体需要设计的界面、UI 功能和交互状态，供后续前端设计稿制作使用。  
> 设计范围：仅包含启动器本体，不包含音频控制、图片压缩、剪贴板历史等具体插件的业务页面。  
> 当前阶段：已按确认的紫色 Material 3 视觉稿完成启动器主框架与插件中心正式接入；插件自身业务页面继续使用现有设计，品牌字体与动画细节留待后续确认。

## 一、全局主框架

所有主要页面共用一个启动器框架。

### 1. 自定义标题栏

需要包含：

- 应用图标和名称；
- 后退、前进按钮；
- 全局搜索入口；
- 命令面板入口；
- 当前任务或通知提示；
- 设置入口；
- 最小化、最大化、关闭按钮；
- 窗口拖拽区域。

### 2. 左侧导航栏

需要包含：

- 概览；
- 收藏；
- 全部工具；
- 最近使用；
- 正在运行；
- 插件管理；
- 设置；
- 导航栏展开和收起；
- 当前页面选中状态。

### 3. 内容区域

需要支持：

- 页面标题与说明；
- 页面级操作按钮；
- 插件内容显示；
- 加载、空白、错误、权限不足等状态；
- 返回上一页；
- 页面滚动。

## 二、主要页面

### 1. 概览首页

首页需要展示：

- 欢迎信息或时间信息；
- 收藏的快捷操作；
- 收藏的小组件；
- 最近使用的工具；
- 正在执行的任务；
- 需要处理的错误或权限提醒；
- 可选的启动器性能摘要；
- “查看全部工具”等快捷入口。

需要设计以下状态：

- 正常内容状态；
- 没有收藏内容；
- 没有最近使用记录；
- 没有正在运行的任务；
- 插件发生异常。

### 2. 全部工具

用于浏览启动器中的所有插件和功能。

需要包含：

- 工具搜索；
- 分类筛选；
- 插件类型筛选；
- 启用状态筛选；
- 排序方式；
- 网格和列表视图切换；
- 工具卡片或列表项。

每个工具需要显示：

- 图标；
- 名称；
- 简短说明；
- 所属分类；
- 插件类型；
- 是否启用；
- 是否收藏；
- 打开页面或执行操作的按钮；
- 更多操作菜单。

### 3. 收藏页面

需要包含：

- 收藏的动作；
- 收藏的页面；
- 收藏的小组件；
- 手动排序；
- 取消收藏；
- 快速执行或打开；
- 没有收藏内容时的引导状态。

### 4. 最近使用

需要包含：

- 最近打开的插件；
- 最近执行的操作；
- 使用时间；
- 再次打开或再次执行；
- 从记录中移除；
- 清空全部记录；
- 没有历史记录时的空状态。

### 5. 正在运行

用于查看所有当前任务和后台服务。

需要分为：

- 当前任务；
- 后台服务；
- 已完成或失败的近期任务。

任务项目需要显示：

- 任务名称；
- 来源插件；
- 当前状态；
- 进度；
- 开始时间；
- 耗时；
- 取消按钮；
- 错误详情入口。

后台服务需要显示：

- 服务名称；
- 来源插件；
- 运行状态；
- 启动时间；
- 最近活动；
- 最近错误；
- 停止或重新启动按钮。

### 6. 插件管理

第一版不需要插件市场，只管理随应用内置的插件。

需要包含：

- 插件列表；
- 搜索和分类筛选；
- 启用或禁用开关；
- 插件排序；
- 插件版本；
- 插件类型；
- 权限数量；
- 后台运行状态；
- 异常或不兼容状态；
- 插件详情入口。

插件详情需要展示：

- 图标、名称和说明；
- 插件版本；
- 包含的动作、页面、小组件和服务；
- 已申请权限；
- 后台运行设置；
- 存储空间占用；
- 最近错误；
- 重新加载插件；
- 清除插件数据；
- 禁用插件。

### 7. 设置页面

#### 常规设置

- 启动后的默认页面；
- 关闭窗口时的行为；
- 是否显示系统托盘；
- 是否恢复上次窗口位置和大小；
- 收藏与最近使用设置。

#### 外观设置

- 浅色、深色、跟随系统；
- 界面缩放；
- 动画开关；
- 减少动态效果；
- 紧凑或标准显示密度。

具体视觉方案等设计稿确定后再实现。

#### 快捷键设置

- 打开启动器；
- 打开全局搜索；
- 打开命令面板；
- 快捷键录入；
- 快捷键冲突提示；
- 恢复默认快捷键。

#### 权限与隐私

- 查看各插件权限；
- 已允许和已拒绝的权限；
- 撤销权限；
- 后台运行权限；
- 文件与剪贴板访问记录；
- 高风险权限说明。

#### 数据与存储

- 数据占用摘要；
- 缓存占用；
- 日志占用；
- 清理缓存；
- 打开数据目录；
- 导出设置；
- 恢复设置；
- 重置启动器。

#### 更新

- 当前版本；
- 检查更新；
- 自动检查更新开关；
- 更新渠道；
- 更新记录；
- 下载和安装进度。

#### 关于

- 应用名称和版本；
- 开源许可；
- 第三方组件；
- 项目说明；
- 问题反馈入口。

## 三、全局功能界面

### 1. 全局搜索

建议使用顶部搜索面板或居中弹出层。

需要包含：

- 搜索输入框；
- 最近搜索；
- 搜索结果分组；
- 插件结果；
- 页面结果；
- 动作结果；
- 设置项结果；
- 收藏结果；
- 键盘上下选择；
- 回车打开或执行；
- 无搜索结果状态。

### 2. 命令面板

需要包含：

- 命令搜索；
- 最近使用的命令；
- 收藏命令；
- 动作命令；
- 页面跳转命令；
- 设置切换命令；
- 命令对应的快捷键；
- 键盘操作提示；
- 命令执行成功或失败反馈。

### 3. 权限请求窗口

普通权限需要显示：

- 请求权限的插件；
- 权限名称；
- 请求原因；
- 允许；
- 拒绝；
- 记住选择。

高风险操作需要更明显的确认界面：

- 即将执行的操作；
- 可能影响的文件或系统内容；
- 风险提示；
- 取消；
- 明确确认按钮。

### 4. 更新界面

需要包含：

- 已是最新版本；
- 发现新版本；
- 更新内容；
- 下载大小；
- 下载进度；
- 安装并重启；
- 稍后提醒；
- 更新失败及重试。

### 5. 通知与反馈

需要设计：

- 操作成功提示；
- 普通提醒；
- 警告；
- 错误提示；
- 可撤销操作；
- 带进度的通知；
- 后台任务完成通知。

## 四、异常和特殊状态

### 1. 插件加载失败

需要显示：

- 插件名称；
- 用户可理解的错误说明；
- 重新加载；
- 禁用插件；
- 查看诊断信息；
- 返回首页。

### 2. 插件运行崩溃

需要显示：

- 当前插件发生异常；
- 主程序仍可继续使用的说明；
- 重新启动插件；
- 打开日志；
- 禁用插件。

### 3. 启动器恢复界面

程序异常退出后再次启动时，需要提供：

- 恢复上次页面；
- 以安全模式启动；
- 暂时禁用异常插件；
- 查看崩溃报告；
- 进入首页。

### 4. 通用状态页面

需要统一设计：

- 加载中；
- 骨架屏；
- 空状态；
- 搜索无结果；
- 网络错误；
- 权限不足；
- 插件已禁用；
- 插件不兼容；
- 页面加载失败；
- 后台任务失败。

## 五、性能与诊断界面

需要展示：

- 启动耗时；
- 当前内存占用；
- 当前 CPU 占用；
- WebView 数量；
- 已加载插件；
- 正在运行的后台服务；
- 调度任务数量；
- 活动监听器数量；
- 最近性能异常；
- 刷新数据；
- 导出诊断报告；
- 打开日志页面。

### 日志页面

需要包含：

- 日志等级筛选；
- 插件筛选；
- 时间范围；
- 关键词搜索；
- 日志详情；
- 复制日志；
- 导出日志；
- 打开日志目录；
- 清理日志。

## 六、插件开发预览界面

该界面供后续开发插件时使用，需要包含：

- 选择插件；
- 选择 Page、Widget 或 Action 入口；
- 页面预览；
- Widget 尺寸切换；
- 浅色或深色背景切换；
- 加载状态模拟；
- 空状态模拟；
- 错误状态模拟；
- 权限不足状态模拟；
- 减少动态效果模拟；
- 重新加载插件；
- 查看插件清单和权限。

## 七、系统托盘菜单

托盘菜单需要包含：

- 显示主窗口；
- 打开全局搜索；
- 打开命令面板；
- 收藏的快捷操作；
- 正在运行的任务数量；
- 暂停或管理后台服务；
- 设置；
- 检查更新；
- 退出程序。

## 八、需要统一设计的基础组件

设计稿还需要覆盖以下通用控件：

- 普通按钮、主按钮、危险按钮、图标按钮；
- 输入框、搜索框、文本区域；
- 下拉选择、复选框、开关、滑块；
- 标签页、分段选择器；
- 卡片、工具卡片、小组件卡片；
- 菜单、右键菜单、气泡提示；
- 弹窗、抽屉、确认窗口；
- Toast、Banner、通知；
- 进度条、加载动画、骨架屏；
- 空状态、错误状态、权限状态；
- 表格、列表、分页；
- 标签、状态标记；
- 滚动条；
- 键盘焦点状态；
- 禁用、加载、选中、警告和错误状态。

## 九、设计优先级

建议设计稿按以下顺序提供：

1. 主窗口、标题栏、侧边导航；
2. 概览、全部工具、插件管理；
3. 插件内容页面外壳；
4. 全局搜索和命令面板；
5. 收藏、最近使用、正在运行；
6. 设置、权限请求和确认弹窗；
7. 加载、空白、错误等通用状态；
8. 性能诊断、日志、更新和插件预览；
9. 托盘与桌面小组件相关界面。

第一批设计稿只要覆盖前六项，就足以开始完成启动器主体的视觉开发。
