# ToolCenter 项目开发规则

## 当前版本范围

- 当前阶段只开发启动器功能与可测试的工程底座。
- 在用户提供正式设计稿前，不得擅自确定品牌配色、视觉风格、动画方案或精细布局。
- 允许保留仅用于操作和测试的语义化基础界面。
- 启动器不得包含音频切换、图片压缩、文本转换等具体插件业务。

## 启动器核心约束

- 保持一个主窗口和一个主 WebView，除非用户明确批准额外窗口需求。
- 插件页面、Widget、Action 和 Service 必须按入口懒加载，禁止在首屏同步加载全部插件代码。
- 公共能力必须保持通用；修改 Plugin Contract 时必须同步评估全部插件。
- 前端不能绕过 Rust 权限层访问高风险系统能力。
- 插件异常不得导致启动器退出，必须提供错误隔离和资源强制清理。
- 不创建不必要的常驻任务、定时器、监听器、后台线程或隐藏窗口。

## 插件开发强制规则

- 插件必须位于独立的 `plugins/<plugin-name>` 目录，并拥有独立清单、`package.json`、README 和测试。
- 插件只能通过 `@tool-center/plugin-contract` 与启动器交互。
- 插件不得直接导入 `@tauri-apps/*`、`apps/desktop` 内部源码、其他插件内部源码或原始 Rust command。
- 插件必须通过 Plugin Context 使用存储、权限、日志、事件、调度和系统能力。
- 插件不得自行调用长期 `setInterval`；周期任务必须注册到共享 Scheduler。
- 所有订阅、监听、快捷键和后台任务必须提供释放流程。
- 插件必须声明权限；权限在前端提示后仍须由 Rust 再次验证。
- 插件页面、Widget 和 Action 入口必须独立分包，不得互相强制加载。
- 设计稿完成后，插件界面必须使用共享 UI Kit 和 Design Tokens，不得注入未审核的全局样式。

## 修改与验收

- 优先进行最小必要修改，不随意重构无关代码。
- 修改后同步检查 README、架构文档、插件文档和迁移说明。
- 完成代码后至少执行类型检查、Lint、单元测试、插件校验、Rust fmt、Rust clippy 和 Cargo test。
- 不得提交密钥、Token、Cookie、账号信息、本地绝对路径或其他个人敏感信息。
- 发布、推送、创建 Release、删除数据或大规模覆盖前必须再次获得用户确认。

## 发布版与插件开发基线

- 项目只维护当前工作区这一套正式源码，不创建按版本号复制的代码目录。
- 默认发布目录是根目录下的 `发布版/`；后续正式构建直接覆盖其中的同名文件。
- 原根目录 `正式版/` 已于 2026-08-11 完成迁移并清理；发布历史和回退只使用 Git 提交与标签，不得重新创建旧发布副本。
- 版本历史和回退使用 Git 提交与标签，不使用 `ToolCenter-vX.Y.Z/` 一类副本保存代码。
- 插件始终在当前项目的 `plugins/<plugin-name>/` 中开发，通过 `.\toolcenter.cmd plugin-dev` 接入真实 ToolCenter 桌面壳进行联调。
- 正式程序不动态加载工作区外的源码或未知第三方插件；插件完成后随 ToolCenter 重新构建并覆盖固定 `发布版/`。
